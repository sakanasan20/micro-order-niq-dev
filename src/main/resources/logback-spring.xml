<configuration debug="true">
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <property name="LOGSTASH_HOST" value="host.docker.internal" />
    <property name="LOGSTASH_PORT" value="5000" />
    <springProperty name="SERVICE_NAME" source="spring.application.name"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] service=${SERVICE_NAME} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${LOGSTASH_HOST}:${LOGSTASH_PORT}</destination>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp />
                <pattern>
                    <pattern>
                        {
                          "service":"${SERVICE_NAME:-unknown}",
                          "level":"%level",
                          "thread":"%thread",
                          "logger":"%logger",
                          "method":"%X{method}",
                          "uri":"%X{uri}",
                          "status":"%X{status}",
                          "durationMs":"%X{durationMs}",
                          "remoteAddr":"%X{remoteAddr}",
                          "message":"%message",
                          "traceId":"%X{traceId:-}",
                          "spanId":"%X{spanId:-}"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>
    
    <logger name="HTTP_REQUEST" level="INFO" additivity="false">
        <appender-ref ref="LOGSTASH"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="LOGSTASH" />
    </root>
</configuration>
